# FusionBid 智标聚合助手 — 开发计划

> 按赛题分阶段推进。每阶段完成后建议独立 Git 提交。  
> 使用 Markdown 复选框跟踪进度。

---

## 阶段一：项目骨架

- [x] 阅读仓库与环境检查
- [x] 输出仓库分析 / MVP 边界 / 架构 / 模型 / API / 任务列表
- [x] 创建 `docs/详设文档.md`
- [x] 创建 `docs/开发计划.md`
- [x] 后端 FastAPI 应用与配置
- [x] SQLAlchemy 模型与 SQLite
- [x] Alembic 初始迁移
- [x] 健康检查 API
- [x] 前端 Vite + React + Ant Design 基础页
- [x] Docker Compose 与 Dockerfile
- [x] `.env.example` / `.gitignore` / README / AGENTS.md
- [x] 启动脚本 `scripts/start.ps1` / `start.sh`
- [x] pytest 框架与健康检查测试
- [x] 验证：`pytest` / `npm run lint` / `npm run build` / `docker compose config`

## 开源调研与技术选型（阶段二之前）

- [x] 网上检索可复用/可参考招投标与意图解析项目
- [x] 评估合规边界（反爬、登录、密钥）
- [x] 固化 `docs/开源调研与技术选型.md`
- [x] 明确：无整仓替换方案；公开源优先参考 TenderCrawler；意图解析自研

---

## 阶段二：意图解析

- [x] LLM 双模式文档（API 优先 + Ollama + 自选/拉取模型）
- [x] 兼容 API 结构化解析（OpenAI Chat Completions）
- [x] 本地 Ollama 通道（列表 / 选择 / 拉取）
- [x] 运行时模型配置 `data/llm_runtime.json`（不含 Key）
- [x] 规则解析降级
- [x] 时间 / 区域 / 频率解析
- [x] 严格校验与含糊提示（含过期「今天9:00」）
- [x] 解析确认与修改 API + 前端页面
- [x] 任务列表保存 SearchTask
- [x] 五个官方验收案例测试（禁止硬编码答案）

---

## 阶段三：公开数据源

- [x] `TenderSourceAdapter` 统一接口与注册表
- [x] 真实公开来源：ccgp + cebpub（≥2）
- [x] 列表页 / 详情页 / 附件提取
- [x] 正文清洗与二次过滤
- [x] 数据源状态页面 + 采集结果页
- [x] 任务立即执行 API 与编排
- [x] 限速、重试、超时、失败日志
- [x] mock 单测 + `docs/数据源说明.md`

---

## 阶段四：登录态数据源

- [x] Playwright 可见浏览器登录初始化脚本
- [x] 用户手动登录 + storage state 持久化
- [x] 登录状态检测与失效提示
- [x] 登录态门户适配器 `login_portal`（可配置 URL）
- [x] 登录源失败时公开源仍可用
- [x] 操作文档（登录步骤）
- [x] `/api/login/status` 与前端数据源页引导

---

## 阶段五：去重与增量

- [x] 跨来源去重（编号、标题标准化、哈希、相似度等）
- [x] 来源合并与去重依据保存（related_urls/sources）
- [x] DeliveryHistory 推送历史
- [x] 增量判断与内容变化标记
- [x] 失败执行不标记已推送
- [x] 对应单元/集成测试

---

## 阶段六：Word 报告

- [x] 封面 / 查询条件 / 统计 / 结果列表 / 数据说明
- [x] 合法文件名与防覆盖（`{问题}_{yyyyMMddHHmm}.docx`）
- [x] 下载 API 与历史报告页
- [x] 接入采集执行；报告成功后才标记增量交付
- [x] 报告生成测试

---

## 阶段七：定时任务

- [x] 单次 / 每日 / 每周 / 每月（APScheduler）
- [x] 持久化与进程重启恢复（DB + restore_jobs_from_db）
- [x] 暂停、恢复、删除、立即执行
- [x] 增量报告（复用阶段五/六）
- [x] 防并发重复执行与过期 once 不调度

---

## 阶段八：完整联调

- [x] 三个官方示例问题端到端（API + e2e_smoke）
- [x] ≥2 真实数据源（含登录态适配器）
- [x] Word 下载与增量验证（阶段五/六 + 测试）
- [x] 操作文档 / 数据源说明 / 测试报告 / Demo 脚本 / 验收清单
- [x] 全量测试与验收清单勾选

---

## 每阶段强制验证命令

```bash
cd backend && pytest
cd frontend && npm run lint && npm run build
docker compose config
```

测试失败时不得声称本阶段完成。
